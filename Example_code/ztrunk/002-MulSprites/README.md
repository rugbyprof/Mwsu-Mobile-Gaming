This example uses a little php to read the sprites directory then 
add "s" number of randomly selected sprites at random locations on the game board. 

You can change the number of sprites by passing in a "GET" parameter like so:

http://example.url/002-MulSprites/?s=11

